machine:
  timezone:
    America/Los_Angeles
  ruby:
    version: 2.2.2
  php:
    verion: 5.5.0
dependencies:
  pre:
    - bash bin/install-wp-tests.sh wordpress_test root '' localhost latest:
        environment:
          WP_VERSION: 4.0.5
  cache_directories:
    - /tmp/wordpress-tests-lib
    - /tmp/wordpress
test:
  override:
    - phpunit